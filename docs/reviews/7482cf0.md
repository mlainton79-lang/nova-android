# codex review — 7482cf0

**Commit:** `7482cf0 fix(ui): stop duplicating user bubble in on-device path (codex P2 on bb0aec7)`
**Base:** `HEAD~1` (`99d6d56`)
**Date:** 2026-07-23

## Verdict

Clean pass. No P0/P1/P2/P3 comments.

> The change removes a duplicate user-message append in the on-device path; the caller already persists the user message before routing to Local Tony. I found no introduced correctness issues in the diff.

## What was addressed

The prior review's P2 (`bb0aec7.md`) flagged that `sendCurrentMessage()` at `MainActivity.kt:736` already appends the user message before delegating to `processThroughBroker()`, then `processOnDevice()` appended the same message again — every Local Tony exchange produced two identical user bubbles. This commit deletes the duplicate append.

## Scope

- `app/src/main/kotlin/com/mlainton/nova/MainActivity.kt:2388` — removed `ChatHistoryStore.appendMessage(this, "user", message)`. `renderChatHistory()` on the following line kept (harmless re-render before the on-device generation thread starts).

## State of the branch

Full frontend brief now landed clean:

- `ed78861` — three good TARGETs: council_health + dark seats rendering, typed failures parsing, debugJson marshalling. Codex P2 on picker trim (functional regression on new users).
- `bb0aec7` — reverted the picker filter. Codex P2 on pre-existing on-device duplicate-append bug.
- `7482cf0` — deleted the duplicate append. Codex clean pass.

`Auto` and `Local Tony` are reachable from the picker; Local Tony exchanges no longer duplicate the user bubble.
