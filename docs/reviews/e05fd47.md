# codex review — e05fd47

**Commit:** `e05fd47 fix(ui): migrate persisted mothballed brains to AUTO on read (codex P2 on 99e3018)`
**Base:** `HEAD~1` (`01af4fa`)
**Date:** 2026-07-23

## Verdict

Clean pass. No P0/P1/P2/P3 comments.

> No discrete, actionable correctness issues were identified in the diff. The change centralizes the mothballed brain list and consistently applies it to preference reads and picker filtering.

## What was addressed

The prior review's P2 (`99e3018.md`) flagged an upgrade-path gap: `DEEPSEEK` and `OPENROUTER` are valid enum names, so `BrokerPrefs.valueOf` returned them cleanly for anyone already persisted there — the app kept routing to a dead seat between opens with no picker rescue. Fixed by treating mothballed persisted values the same as unknown ones.

## Scope

- `app/src/main/kotlin/com/mlainton/nova/BrainBroker.kt` — new top-level `val MOTHBALLED_BRAINS: Set<BrainMode> = setOf(BrainMode.DEEPSEEK, BrainMode.OPENROUTER)`. Single source of truth; extend when retiring more seats.
- `app/src/main/kotlin/com/mlainton/nova/BrokerPrefs.kt:17-24` — `getBrainMode` now resolves the enum first, then maps any mothballed result to `AUTO`. Migration is passive: no writeback, so the pref file stays intact and any future un-mothballing (adding a seat back) re-enables persisted users automatically.
- `app/src/main/kotlin/com/mlainton/nova/MainActivity.kt:534-535` — `showBrainPicker` now references the shared `MOTHBALLED_BRAINS` constant instead of a locally scoped `setOf(...)`. Removes the "two places to update on future mothballs" trap.

## Sandbox validation

Codex ran `bash ./gradlew testDebugUnitTest -q` and hit the same AAPT2 x86_64-on-ARM-PRoot env failure at `processDebugResources` — no Kotlin type-check possible in this sandbox. Same env issue as prior cycles. Change is small (17 lines), touches three files with no parallel structures, and codex's review was based on reading the diff directly.

## State of the branch

Picker trim is now fully correct:

- `bcd6455` → `99e3018` — trim itself corrected (mothballed brains hidden; AUTO + LOCAL_TONY + real seats kept). Fallback default changed from `CLAUDE_MOCK` to `AUTO`.
- `99e3018` → `e05fd47` — mothballed persisted values migrated to `AUTO` on read. No user stays on a dead seat between opens.

`Auto`, `Local Tony`, `Gemini`, `Claude`, `OpenAI`, `Groq (Llama 4)`, `Mistral`, and `Council` are the picker's live entries.
