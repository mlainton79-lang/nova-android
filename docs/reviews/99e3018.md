# codex review — 99e3018

**Commit:** `99e3018 fix(ui): trim mothballed brains from picker (deepseek/openrouter/xai), keep auto+local`
**Base:** `HEAD~1` (`bcd6455`)
**Date:** 2026-07-23

## Verdict

Trim itself is right this time (correcting cycle 1's inverted trim). Codex flagged one **P2** on the upgrade path for users already persisted on a mothballed mode.

## Comments

### P2 — Migrate saved mothballed brain modes
**File:** `app/src/main/kotlin/com/mlainton/nova/BrokerPrefs.kt:17-20`

> If a user already has `DEEPSEEK` or `OPENROUTER` stored in preferences, `valueOf` still returns that valid enum, so the app continues routing messages to the now-hidden/dead provider until the user manually opens the picker and chooses another mode. Since this patch hides those modes specifically to prevent dead-seat selection, `getBrainMode` should also map the mothballed persisted values to `AUTO`.

**Assessment:** valid, and directly aligned with the brief. The brief said:

> Routing enum stays untouched (removed modes must still deserialise from old saved prefs — verify the saved-pref fallback to AUTO from the original brief still holds, add it if absent).

The fallback is now in place for **unknown** names, but `DEEPSEEK` / `OPENROUTER` are still **known valid** enum entries — they deserialise cleanly and route straight through to a dead backend seat. The picker also can't rescue them: `showBrainPicker` computes `selectedIndex = modes.indexOf(currentBrainMode)`, which returns `-1` when the current mode was filtered out; no radio is preselected and the user has to know to override it. Between app opens they keep routing to a mothballed brain.

The minimal fix: extend `BrokerPrefs.getBrainMode` to check if the resolved mode is in the mothballed set and return `AUTO` instead. The mothballed set lives inside `showBrainPicker` today — could hoist to a shared constant on `BrainMode` or `BrokerPrefs`.

**Forks:**
- **A. Fix now** — small follow-up commit that migrates persisted mothballed modes to AUTO. Closes the gap the brief explicitly cared about. My recommendation.
- **B. Park it** — the P2 only affects users who actively picked DeepSeek or OpenRouter before this patch. Since those seats are dead, they were already broken; the routing just continues to fail silently instead of being auto-corrected.

Stopping for direction.

## What passed clean

- Picker filter (`MainActivity.kt:537-538`) — mothballed set applied, other modes untouched, no parallel label/ordering arrays to keep in sync.
- Fallback default changed from `CLAUDE_MOCK` to `AUTO` in `BrokerPrefs`.
- Comments explain the invariant (why the enum entries stay, why AUTO is the safe default).

## Delta from cycle 1

`ed78861`'s trim inverted the brief — it removed AUTO and LOCAL_TONY (the keeps) instead of the mothballed brains. That trim was reverted in `bb0aec7`. This commit applies the correct trim: only `DEEPSEEK` and `OPENROUTER` are hidden (no `GROK`/`XAI` enum entry ever existed). All of `AUTO`, `LOCAL_TONY`, `GEMINI_MOCK` (Gemini), `CLAUDE_MOCK` (Claude), `OPENAI_LIVE` (OpenAI), `GROQ`, `MISTRAL`, `COUNCIL_MOCK` (Council) remain selectable — matches the brief exactly.
